* selects all element 
boxsizing->how box dimension is calculated
:root->pseudo class which define scope of variable, equivalent to html selector
-- -> use to declare variable
var() -> use to use variable
rem -> unit of size, relative to font size of root element 
em -> unit of size, relative to font size of current element 
utility class -> 
mix blend mode -> to remove background or play with it 
